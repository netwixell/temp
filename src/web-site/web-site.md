```bash

block-name/               # Папка блока.
  img/                    # Изображения, используемые блоком и обрабатываемые автоматикой сборки.
  bg-img/                 # Изображения для использования в стилях (не обрабатываются автоматикой сборки).
  block-name.pug          # Разметка (pug-примесь, отдающая разметку блока, описание API примеси).
  block-name.blade.php    # Разметка blade повторяющая разметку из block-name.pug
  block-name.sass         # Стилевой файл блока.
  block-name.js           # js-файл блока.
  block-name--mod.sass    # Отдельный стилевой файл БЭМ-модификатора блока.
  block-name--mod.js      # js-файл для отдельного БЭМ-модификатора блока.
  block-name.md           # Описание для документации, подсказки.

svg-sprite/svg/           # Папка с svg-файлами для создания спрайта
favicon/img/              # Папка для favicon.ico и дополнительных файлов favicon

page-name.pug             # Файл разметки pug с подключенными блоками страницы сайта
page-name.blade.php       # Файл разметки blade с подключенными блоками страницы сайта
page-name.js              # Файл с импортом скриптов для страницы сайта

scripts.js                # Скрипты для всех страниц сайта
style.sass                # Стили для всех страниц сайта

```
